#include <stdio.h>

int main() {
    int marks[5], i;
    for (i = 0; i < 5; i++) {
        printf("Enter mark %d: ", i+1);
        scanf("%d", &marks[i]);
    }

    printf("Marks are:\n");
    for (i = 0; i < 5; i++)
        printf("%d ", marks[i]);

    return 0;
}
