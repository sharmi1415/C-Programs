#include <stdio.h>

int main() {
    int sales[7], i, total = 0;

    for (i = 0; i < 7; i++) {
        printf("Enter sales for day %d: ", i+1);
        scanf("%d", &sales[i]);
        total += sales[i];
    }

    printf("Total weekly sales = %d", total);
    return 0;
}
