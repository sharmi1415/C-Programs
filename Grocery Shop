#include <stdio.h>

int main() {
    int quantity;
    float price, total;

    printf("Enter quantity: ");
    scanf("%d", &quantity);

    printf("Enter price per item: ");
    scanf("%f", &price);

    total = quantity * price;

    printf("Total bill amount = %.2f\n", total);

    return 0;
}
