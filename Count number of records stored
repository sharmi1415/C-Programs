#include <stdio.h>

struct Student {
    int id;
    char name[20];
};

int main() {
    struct Student s[50];
    int n;

    printf("Enter number of records: ");
    scanf("%d", &n);

    printf("Number of records stored: %d\n", n);

    return 0;
}
